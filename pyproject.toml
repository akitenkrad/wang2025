[project]
name = "wang2025-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "culture-llm-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
culture-llm-tools = { workspace = true }
